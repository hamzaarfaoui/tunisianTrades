{% extends "base.html.twig" %}
{% block title %}{% endblock %}
{% block stylesheets %}
    <link type="text/css" rel="stylesheet" href="{{ asset('e-shop/css/nav.css') }}" />
    <style>
        .section {
            margin-top: -62px;
        }
    </style>
{% endblock %}
{% block body %}
    <div id="navigation">
        <!-- container -->
        <div class="container">
            <div id="responsive-nav">
                <!-- category nav -->
                <div class="category-nav">
                    <span class="ct">Catégories <i class="fa fa-list"></i></span>
                    <ul class="menu">
                        {% for categorie in categories %}
                        <li class="dropdown side-dropdown">
                            <a class="dropdown-toggle" href="#">{{ categorie.name }}</a>
                            <div class="megadrop">
                                {% for sc1 in categorie.categories %}
                                    <div class="col">
                                         <h3>{{ sc1.name }}</h3>
                                        <ul>
                                            {% for souscat in sc1.sousCategories %}
                                                <li><a href="#">{{ souscat.name }}</a></li>
                                            {% endfor %}
                                        </ul>
                                        <hr class="hidden-md hidden-lg">
                                    </div>
                                {% endfor %}
                            </div>
                        </li>
                        {% endfor %}
                    </ul>
                </div>
                <!-- /category nav -->

                <!-- menu nav -->
                
                <!-- menu nav -->
            </div>
        </div>
        <!-- /container -->
    </div>
    <!-- HOME -->
    <div id="home">
        <!-- container -->
        <div class="container">
            <!-- home wrap -->
            <div class="home-wrap">
                <!-- home slick -->
                <div id="home-slick" style="margin-bottom: 50px;margin-top: -31px;">
                    {{ render(controller('App\\Controller\\Back\\SlidersController::listInFront')) }}
                </div>
                <!-- /home slick -->
            </div>
            <!-- /home wrap -->
        </div>
        <!-- /container -->
    </div>
    <!-- /HOME -->

    <!-- section banners three in line-->
    {#<div class="section">
        <!-- container -->
        <div class="container">
            <!-- row -->
            <div class="row">
                <!-- banner -->
                <div class="col-md-4 col-sm-6">
                    <a class="banner banner-1" href="#">
                        <img src="{{asset('e-shop/img/banner10.jpg')}}" alt="" style="height: 180px;">
                        <div class="banner-caption text-center">
                            <h4 class="white-color">NEW COLLECTION</h4>
                        </div>
                    </a>
                </div>

            </div>
            <!-- /row -->
        </div>
        <!-- /container -->
    </div>#}
    <!-- /section -->
    <!-- start categories -->
        {% include 'includes/front/index/categories.html.twig' %}
    <!-- end categories -->
   
{% endblock %}
{% block javascripts %}
<script>
    {% for categorie in sc2 %}
        $('#product-slick-{{categorie.id}}').slick({
    slidesToShow: 5,
    slidesToScroll: 1,
    autoplay: false,
    infinite: true,
    speed: 300,
    dots: true,
    arrows: false,
    appendDots: '.product-slick-dots-1',
    responsive: [{
        breakpoint: 991,
        settings: {
          slidesToShow: 1,
          slidesToScroll: 1,
        }
      },
      {
        breakpoint: 480,
        settings: {
          dots: false,
          arrows: true,
          slidesToShow: 1,
          slidesToScroll: 1,
        }
      },
    ]
  });
    {% endfor %}
    </script>
{% endblock %}
